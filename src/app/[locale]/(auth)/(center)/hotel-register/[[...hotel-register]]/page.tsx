import RegistrationForm from "./components/RegistrationForm";

const page = () => {
  return (
    <div className="w-full bg-gray-100">
      <RegistrationForm />
    </div>
  );
};

export default page;
